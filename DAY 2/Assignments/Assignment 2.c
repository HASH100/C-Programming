// Write a program to find LCM 
/*
 Initialize A and B and LCM
 GET A & B from user
   max = max(A, B)
   WHILE TRUE
      IF max is divisible by A and B THEN
         LCM = max
         BREAK
      ENDIF
      Increment max
   END WHILE
   DISPLAY LCM
*/